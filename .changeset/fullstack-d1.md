---
"t-stack-manager": patch
---

The bundled `tanstack-fullstack` template now uses Cloudflare D1 (`dbSetup: d1`) instead of Turso, matching its Cloudflare web deploy so app and database live on one platform.
