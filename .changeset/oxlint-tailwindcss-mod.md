---
"t-stack-manager": patch
---

Add a bundled `add-oxlint-tailwindcss` modification that installs `oxlint-tailwindcss` and wires its plugin, mandatory `entryPoint`, and rules into `.oxlintrc.json`. Linked as a default modification on the `tanstack-fullstack` template so it applies automatically on scaffold.
