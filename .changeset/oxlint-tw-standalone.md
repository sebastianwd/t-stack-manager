---
"t-stack-manager": patch
---

The `add-oxlint-tailwindcss` modification is now a standalone opt-in default: it is no longer linked to the `tanstack-fullstack` template's `default_modifications`, so scaffolds do not apply it automatically. Ask for it explicitly to apply it.
