---
"t-stack-manager": patch
---

Scaffold now asks for the project name and target directory as plain text instead of a multiple-choice picker (which rejected free-form answers), and offers the current directory as a target option when no default is saved.
