---
"t-stack-manager": patch
---

Scaffold now uses a multiple-choice picker to select the template (a discrete choice), while keeping the project name and target path as plain-text questions. Clarifies the earlier guidance that had the agent avoiding the picker everywhere.
